block variables
doctype html
html
  head
    meta(charset="utf-8")
    link(rel="shortcut icon", href="/favicon.ico")
    meta(name="viewport", content="width=device-width, initial-scale=1, shrink-to-fit=no")
    meta(name="theme-color", content="#000000")
    style 
      include css/style.min.css
    title #{title} - blurpledit
  body
    div#root
      section.navbar
        div#rootButton.rootButton 
          a(href='/') b
        div#searchBar.searchBar
          form#searchForm
            input#searchInput.searchInput(type='text')
        div.privacyToggleContainer
          label.toggle
            input(type='checkbox')
            span.slider
      section.content
        div#content
          block layout-content